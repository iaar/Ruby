<% content_for(:title) { 'Who We Are' } %>
<h1>Who We Are</h1>
 
<table>
  <% @persons.each do |persons| %>
  	<tr>
    	<th><%= image_tag generation_link_to_gravatar(persons.email) %></th>
    	<th><%= generation_name(persons.email) %></th>
	</tr>
  <% end %>
</table>